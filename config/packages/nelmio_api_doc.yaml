nelmio_api_doc:
    documentation:
        info:
            title: My App
            description: This is an awesome app!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/project(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/activity(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/Backlog(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/comments(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/priority(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/status(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/users(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/secured/userStory(?!/doc$) # Accepts routes under /api except /api/doc
