{% extends 'base.html.twig' %}

{% block body %}

    <div id="root"><div>
{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            <script type="text/babel">
             var notesUrl = '{{ path('priority_priorityList') }}';
         ReactDOM.render(
          <Contacts url={notesUrl} />
          );
</script>

{% endblock %}